;; the first version produces the reverse because he runs cons on the first
;; element of the list against answer, so it becomes the last element

;; the second one will not create a list, it will return a list from the RHS

;; funnily enough, I did both of these mistakes when doing exercise 2.18!
